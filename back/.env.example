
# Example environment variables for backend (DO NOT COMMIT real secrets)
# Set PORT to the value your app listens on (app.js uses 3001 by default)
PORT=3001

# Database connection
# If you use the included docker-compose service use DB_SERVER=db
# To connect from a container to a SQL Server running on the Windows host, use host.docker.internal
DB_USER=sa
DB_PASSWORD=LMOOANM/*2011
DB_SERVER=db
DB_PORT=1433
DB_NAME=storeManagement

# JWT secret used to sign tokens (removed - not used in this project)
# Email credentials (used by nodemailer in the app)
EMAIL_USER=olivernie2626@gmail.com
EMAIL_PASS=rtlwhkeyzachylvi

# Notes:
#  - Copy this file to .env and fill real values for local development.
#  - Never commit .env with real secrets to source control.
#  - If you previously committed secrets, rotate them (DB passwords, email passwords, JWT secret).

# SECURITY NOTE:
# The EMAIL_PASS shown here should be an App Password (16-character) generated from your Google
# account when using 2-Step Verification. Avoid storing real passwords in files checked into
# version control. For production consider using Docker secrets, environment variables in a
# secret manager, or an SMTP provider (SendGrid, Mailgun) with API keys.

